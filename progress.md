# Progress Log - Option Levels Trading System

Файл отслеживания прогресса разработки и состояния проекта.

---

## 1. Текущее верифицированное состояние (Current Verified State)
* **Корневая директория репозитория:** `c:\Users\circlealgorythm\.\antigravity\bot_grid`
* **Стандартный путь запуска пайплайна:** `C:\Users\circlealgorythm\AppData\Local\Programs\Python\Python311\python.exe main.py`
* **Стандартный путь запуска дашборда:** `C:\Users\circlealgorythm\AppData\Local\Programs\Python\Python311\python.exe Dashboard/run_dashboard.py`
* **Стандартный путь верификации (тесты):** `C:\Users\circlealgorythm\AppData\Local\Programs\Python\Python311\python.exe -m pytest tests/`
* **Наивысший приоритет нереализованной фичи:** Реализация индикации алертов и уровня Gamma Flip (Strike Flip) согласно Roadmap.
* **Текущий блокер:** Нет.

---

## 2. Журнал сессий (Session Record)

### Сессия: 2026-06-11
* **Цель:** Проверить работоспособность парсера/индикатора, устранить баги, внедрить Harness, а также реализовать автоматическую синхронизацию уровней из GitHub в Веб-Дашборд (Вариант Б).
* **Выполнено:**
  * Успешно запущен локальный пайплайн сбора уровней `main.py` под правильным интерпретатором Python 3.11. Все уровни (EUR, GBP, XAU, NAS, SPX, BTC, USDCAD) успешно сгенерированы и скопированы в папки MT5.
  * Выявлена и исправлена ошибка в функции `copy_csv_to_mt5` (игнорировался параметр `mt5_gex_dir` и не возвращался путь), приводившая к падению теста `test_copy_csv_to_mt5_uses_configured_directory`.
  * Успешно пройдены все 13 локальных юнит-тестов (100% passed).
  * Составлена подробная дорожная карта развития системы в файле `roadmap.md`.
  * Внедрена методология Harness: созданы файлы `GEMINI.md`, `progress.md`, `feature_list.json`, `init.sh`, `init.ps1` и `clean-state-checklist.md`.
  * Реализован Вариант Б синхронизации в `Dashboard/run_dashboard.py`: при входящих API-запросах дашборд автоматически скачивает недостающие файлы уровней за сегодня с GitHub и копирует их в терминал MT5. Добавлен 15-минутный троттлинг запросов к GitHub для предотвращения сетевого шума и фризов интерфейса.
* **Запущенная верификация:** `python -m pytest tests/` (все тесты зеленые), проверка синтаксиса `py_compile`.
* **Зафиксированные доказательства:** Прохождение тестов подтверждено выводом терминала; файлы CSV генерируются и скачиваются; дашборд скомпилирован успешно и протестирован.
* **Известные риски:** Риск отсутствия интернета у пользователя при первом запросе нового дня (дашборд не сможет стянуть уровни и выведет предупреждение, но повторит попытку через 15 минут).
* **Последнее обновление:** Успешно добавлена поддержка S&P 500 (SPX) и USDCAD во все модули системы, внедрен точный расчет Gamma Flip на основе метода Брента. Устранена ошибка дублирования ежедневной аналитики для SPX и USDCAD по дням недели. Проведена унификация терминологии: уровень "Gamma Flip" переименован в "Zero Gamma" во всей системе (индикатор, инструкция). Сгенерирован премиальный неоновый логотип и установлен в качестве фавиконки (`favicon.png`) для веб-дашборда с корректным подключением в HTML-разметку.

### Сессия: 2026-06-12
* **Цель:** Решить проблему отсутствия уровней за 12 июня на графиках MT5, исправить парсинг Nasdaq для второй недели месяца, запустить веб-дашборд и устранить баг кэширования графических объектов при перезапуске/смене таймфрейма в MT5.
* **Выполнено:**
  * Выявлен и исправлен баг в `select_daily_contracts()` для Nasdaq в `main.py`: внедрено динамическое вычисление кода недели (`QN1`-`QN4` или `QN` для EOM) вместо жестко прописанного кода первой недели `QN1`. Это восстановило расчет уровней Nasdaq на сегодня (Week 2, код `QN2`).
  * Вручную запущен локальный пайплайн `main.py` под Python 3.11: сгенерированы уровни по всем 7 активам (EUR, GBP, XAU, NAS, SPX, BTC, USDCAD) на 12 июня 2026 года и скопированы в терминалы MT5.
  * Устранен критический баг кэширования в `CME_GEX_Levels_Indicator.mq5`: добавлена функция `NeedUpdateLevels()`, которая проверяет дату в лейбле `UpdateStatus`. Если сегодня рабочий день и данных за сегодня нет на графике (или статус равен MISSING), индикатор принудительно обновляет уровни, решая проблему застревания на старых вчерашних уровнях.
  * Оптимизировано определение базовой даты в MQL5: теперь используется `MathMax(TimeLocal(), TimeCurrent())` для обхода отставания серверного времени брокера в утренние часы.
  * Реализован сдвиг выходных дней к предшествующей пятнице в MQL5, благодаря чему статус `today_loaded` показывает `OK` на выходных.
  * Проведена компиляция индикатора в `CME_GEX_Levels_Indicator.ex5` через `MetaEditor64.exe` без ошибок и варнингов, файлы скопированы в терминал MT5.
  * Успешно перезапущен терминал MT5 и верифицировано появление всех уровней за 12 июня на графиках EURUSD, GBPUSD, XAUUSD, USDCAD, BTCUSD, Nasdaq100 и S&P500. Красный статус `GEX today MISSING` сменился на `GEX today OK: 2026-06-12`.
* **Запущенная верификация:** Тесты `pytest tests/` (15 passed).
* **Зафиксированные доказательства:** Уровни за 12 июня успешно отрисовались на всех чартах MT5.
* **Известные риски:** Нет.
* **Следующий шаг:** Проверить автогенерацию аналитических отчетов Scheduled Tasks.

### Сессия: 2026-06-15
* **Цель:** Выполнить ежедневный анализ GEX-уровней и сгенерировать аналитический отчет под текущий день недели (понедельник).
* **Выполнено:**
  * Успешно запущен локальный скрипт извлечения GEX-метрик для всех 7 активов (EUR, GBP, XAU, NAS, SPX, BTC, USDCAD) на 15 июня 2026 года.
  * Получены текущие спот-котировки с Yahoo Finance и вычислены точные форвардные смещения (`fw_offset = Spot - Futures_Spot`).
  * Произведен математический расчет уровней в спот-котировках MT5 с учетом премий (`Spot MDD = Strike + Offset +/- Settle` для Daily MDD, `Spot = Level + Offset` для остальных зон).
  * На основе полученных метрик и шаблона `GEX_Analysis_Template.md` подготовлен профессиональный ежедневный обзор на русском языке с физикой рехеджирования маркет-мейкеров, блоками "Что это значит", "Вывод" и торговыми планами.
  * Обновлен файл `Dashboard/analysis.json`: интегрирован понедельничный отчет под ключом `"monday"`, сброшены будущие дни недели (`tuesday` - `friday`) и недельный отчет в `""` (в связи с началом нового недельного цикла), обновлена временная метка `updated_at`.
* **Запущенная верификация:** Проверен синтаксис JSON-файла, проверены математические формулы уровней и соответствие шаблону.
* **Зафиксированные доказательства:** Файл `Dashboard/analysis.json` успешно перезаписан и проверен.
* **Известные риски:** Нет.

### Сессия: 2026-06-16
* **Цель:** Переработка визуального отображения инфографики Net GEX и гамма-профилей в веб-дашборде под стиль предоставленного референса (сглаженные волны Calls/Puts с заливкой градиентом).
* **Выполнено:**
  * Добавлен клиентский маппинг размеров контрактов опционов по всем торгуемым валютам и индексам для точной нормализации абсолютных величин.
  * Реализовано математическое разделение `Total_GEX` и `Total_Abs_Gamma` на отдельные компоненты: `Call GEX` и `Put GEX` непосредственно во фронтенд-коде.
  * Созданы плавные кривые (tension 0.4) для волн Call/Put с индивидуальными полупрозрачными градиентами (синий и оранжевый) на правой оси ординат `yGamma`.
  * Перерисованы столбцы чистого GEX (Net GEX) в яркие контрастные цвета: ослепительно-голубой/неоновый (Calls) и красный (Puts).
  * Изменен стиль разметки цены Spot в `verticalLinePlugin` с штрихового голубого на тонкий сплошной золотистый с выводом текстового значения котировки сверху (`Price: <значение>`).
  * Полностью обновлена легенда графика в `index.html` и переименован пункт в меню настроек.
* **Запущенная верификация:** Прохождение всех 15 тестов `pytest tests/` (100% passed).
* **Зафиксированные доказательства:** Успешный запуск веб-дашборда и отрисовка на всех 7 парах.
* **Известные риски:** Нет.

### Сессия: 2026-06-16 (Анализ плейлиста и руководство GEX)
* **Цель:** Изучить 42 транскрипта из плейлиста YouTube и составить подробное руководство по опционной торговой стратегии GEX в отдельном файле в корне проекта.
* **Выполнено:**
  * Проведен анализ 42 транскриптов обучающих и еженедельных видеороликов, содержащих логику торговли опционными уровнями, динамику рехеджирования маркет-мейкеров и закономерности волатильности (+GEX против -GEX).
  * Создан файл `GEX_Trading_Strategy_Guide.md` в корне проекта на профессиональном русском языке. В документе описаны: физика работы маркет-мейкеров, роли дилеров как дельта-нейтральных игроков, Zero Gamma (Gamma Flip), Absolute Gamma (AG) и LMP (Line of Max Power) с Power Zone (PZ).
  * Разработаны детальные инструкции для 4 основных торговых сетапов: диапазонная торговля (Iron Condor), отскок от дневных уровней поддержки/сопротивления (через продажу кредитных спредов Bear Call / Bull Put), возврат цены к магниту LMP, и хеджированные комбинации (IC + Calendar Put Spread / Broken-Wing Butterfly).
  * Описаны правила риск-менеджмента и управления 0 DTE опционами (вход спустя 30-60 минут после открытия, закрытие позиций при достижении 4-5% доходности портфеля в неделю).
* **Запущенная верификация:** Запуск юнит-тестов `pytest tests/` (15 passed, 100% успеваемость).
* **Зафиксированные доказательства:** Файл `GEX_Trading_Strategy_Guide.md` успешно записан в корень проекта, внесены записи в `todo.md`, `lessons.md` и обновлен статус фичи в `feature_list.json`.
* **Известные риски:** Нет.

### Сессия: 2026-06-16 (Адаптация цен под спот в Дашборде)
* **Цель:** Реализовать автоматический пересчет и сдвиг всех опционных уровней и страйков дашборда под спотовые цены для удобства торговли.
* **Выполнено:**
  * Реализовано получение и кэширование (60s TTL) текущих спот-котировок с Yahoo Finance для всех 8 активов (EURUSD=X, GBPUSD=X, USDCAD=X, BTC-USD, ETH-USD, ^SPX, ^NDX, GC=F) в `Dashboard/run_dashboard.py`.
  * Добавлена передача `live_spot` и `live_offset` в объекте `metadata` при GET-запросах `/api/data`.
  * Добавлен интерактивный чекбокс `"Адаптировать под спот"` в настройки отображения дашборда (`index.html`) с сохранением состояния в `localStorage`.
  * Обновлена логика `Dashboard/app.js` для динамического сдвига всех уровней (карточки метрик, опционная сетка, оси графиков и плагин вертикальных линий) на величину `live_offset`.
* **Запущенная верификация:** Запуск юнит-тестов `pytest tests/` (15 passed), запуск и проверка пайплайна `main.py`, успешная проверка API-ответов сервера.
* **Зафиксированные доказательства:** Сервер успешно отдает `live_spot` и `live_offset`, все 15 тестов пройдены успешно, интерфейс корректно перерисовывается при переключении чекбокса.
* **Известные риски:** Риск временного отсутствия котировок с Yahoo Finance при сбоях сети (в этом случае сдвиг равен 0.0, интерфейс остается стабильным в котировках фьючерса).

### Сессия: 2026-06-16 (Интеграция Power Zone / Зона Силы)
* **Цель:** Интеграция Зоны Силы (Power Zone / PZ) — 70% Value Area распределения абсолютной гаммы вокруг LMP — во все модули системы (Python-пайплайн, MQL5-индикатор и веб-дашборд).
* **Выполнено:**
  * **Математика и Пайплайн (`main.py`):** Реализована функция `calculate_power_zone` для поиска 70% Value Area абсолютной гаммы, центрированной вокруг страйка LMP. В выходной DataFrame добавлены колонки `PZ_High` и `PZ_Low`.
  * **Парсер Дашборда (`Dashboard/run_dashboard.py`):** Добавлен парсинг полей `PZ_High` и `PZ_Low` из CSV-файлов с передачей их в метаданных JSON API как `pz_high` and `pz_low`.
  * **Интерфейс Дашборда (`Dashboard/index.html` и `Dashboard/app.js`):** Добавлен тумблер настроек "Показывать Power Zone" и карточка метрики PZ. В таблицу Calculated Levels внедрены границы "PZ Вверх" и "PZ Низ". В плагин вертикальных линий Chart.js добавлена отрисовка золотистого заполненного прямоугольника (opacity 0.08) с пунктирными границами.
  * **MQL5 Индикатор (`CME_GEX_Levels_Indicator.mq5`):** Внедрены параметры `InpDrawPZ` и `InpColorPZ`. Обновлен разбор CSV (колонки index 18 и 19) и отрисовка заполненного фонового прямоугольника `OBJ_RECTANGLE` под графиком цены.
  * **Сборка и Деплой:** Индикатор успешно скомпилирован в MetaEditor без ошибок и предупреждений (0 errors, 0 warnings) и скопирован в MT5 через `copy_mt5.py`. Запущен пайплайн `main.py` для обновления уровней всех активов с поддержкой PZ.
* **Запущенная верификация:** Запуск юнит-тестов `pytest tests/` (15 passed), компиляция индикатора, копирование файлов.
* **Зафиксированные доказательства:** Успешный прогон `pytest`, скомпилирован `CME_GEX_Levels_Indicator.ex5`, обновлены файлы CSV во всех каталогах MT5, интерфейс дашборда адаптирован.
* **Известные риски:** Нет.

### Сессия: 2026-06-16 (Удаление функционала Power Zone / Зона Силы)
* **Цель:** Полный откат и удаление функционала Power Zone (PZ / Зона Силы) по требованию пользователя во всех компонентах системы (Python-пайплайн, MQL5-индикатор и веб-дашборд).
* **Выполнено:**
  * **Пайплайн (`main.py`):** Полная регенерация CSV-файлов уровней без колонок `PZ_High` и `PZ_Low` (возвращено к исходному составу столбцов).
  * **Интерфейс Дашборда (`Dashboard/app.js`):** Удалена переменная `showPZ` и все упоминания Power Zone из состояния. Подтверждена чистота файлов `Dashboard/index.html` и `Dashboard/app.js` от кода отрисовки или элементов управления PZ.
  * **MQL5 Индикатор (`CME_GEX_Levels_Indicator.mq5`):** Подтверждено отсутствие параметров, разбора колонок и объектов рисования PZ (код полностью соответствует pre-PZ коммиту `654f820`). Произведена чистая перекомпиляция индикатора в `CME_GEX_Levels_Indicator.ex5` (0 ошибок, 0 предупреждений) и деплой во все терминалы MT5.
  * **Веб-Сервер (`Dashboard/run_dashboard.py`):** Перезапущен сервер дашборда на порту 8080 для работы с обновленными CSV без колонок PZ.
  * **Документация и логи:** Удалена фича из `feature_list.json`, скорректированы `tasks/todo.md` и `tasks/lessons.md`.
* **Запущенная верификация:** Запуск юнит-тестов `pytest tests/` (15 passed), проверка компиляции MQL5, проверка работы веб-сервера.
* **Зафиксированные доказательства:** Все 15 тестов пройдены, CSV-файлы успешно перегенерированы без PZ колонок и скопированы в MT5, сервер запущен, индикатор скомпилирован без PZ.
* **Известные риски:** Нет.

### Сессия: 2026-06-16 (Динамический фильтр шума GEX в MQL5)
* **Цель:** Исправить ошибку отсутствия уровней на индексах Nasdaq100 (NAS) и SPX500 (SPX), а также золоте (XAU) и криптовалютах (BTC) в терминале MT5, вызванную жестким шумовым фильтром.
* **Выполнено:**
  * **Анализ первопричины**: Выявлена фундаментальная разница в масштабах GEX между валютными парами (EUR/GBP/CAD, где значения измеряются в миллионах и миллиардах) и индексами/криптой/золотом (где значения измеряются от 0.029 до 2400). Из-за этого жесткий фильтр `InpMinGexFilter = 1000.0` полностью отсекал все уровни для активов NAS, XAU, BTC и практически все для SPX.
  * **Адаптивный фильтр шума**: В MQL5 индикатор `CME_GEX_Levels_Indicator.mq5` встроен предварительный проход по строкам CSV для вычисления максимального absolute GEX файла (`file_max_abs_gex`).
  * **Автопереход на относительный порог**: Если `InpMinGexFilter` превышает `file_max_abs_gex` (или равен 0), индикатор автоматически переходит на адаптивную фильтрацию шума, равную 2% от `file_max_abs_gex`, аналогично логике веб-дашборда.
  * **Сборка и Деплой**: Индикатор успешно скомпилирован через `MetaEditor64.exe` (0 ошибок, 0 предупреждений) в `CME_GEX_Levels_Indicator.ex5` и скопирован во все терминалы.
  * **Прогон Пайплайна**: Перезапущен пайплайн `main.py` под Python 3.11: все уровни за `2026-06-16` перегенерированы и скопированы во все терминалы MT5.
* **Запущенная верификация:** Прогон юнит-тестов `pytest tests/` (15 passed, 100% успеваемость), верификация компиляции MQL5 и генерации файлов.
* **Зафиксированные доказательства:** Успешный компиляционный лог, сгенерированные CSV за 16 июня по всем 7 активам размещены в каталогах MT5, тесты пройдены.
* **Известные риски:** Нет.
* **Следующий шаг:** Дальнейший мониторинг и ручная торговля в терминале.


